{
    "type": "object",
    "properties": {
        "outputFormat": {
            "required": true,
            "default": "json",
            "enum": [
                "json",
                "xml"
            ],
            "type": "string"
        }
    }
}